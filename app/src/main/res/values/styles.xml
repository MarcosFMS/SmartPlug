<resources>

    <!-- Base application theme. -->
    <style name="SmartPlugTheme" parent="SmartPlugTheme.Base">

    </style>

    <style name="SmartPlugTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SmartPlugTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="SmartPlugTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
